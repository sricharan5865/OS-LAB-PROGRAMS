# Skill Activity 03: Git Version Control & Project Directory Organization

## Aim & Overview
Git initialization, staging, commits, branch management, status inspection, and structured project directory layout (src, include, bin, docs).

## Implementation (`main.c`)
```c
// Skill 03: Git Repository & Workspace Setup
#include <stdio.h>

int main() {
    printf("Git Repository setup & version control verified.\n");
    return 0;
}
```

## Compilation & Execution
```bash
make
./main
```

## Output & Verification
```text
$ git status
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

## Key Learning Points
Configured local and remote Git repository synchronization adhering to OSSP course specifications.
